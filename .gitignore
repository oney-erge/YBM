__pycache__/
.setup/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
.env
.env.*
!.env.example
config/config.yaml
config/config.yaml.bak-*
agent_control.db
.agent_control/
*.session
*.session-journal
*.pem
*.key
*.p12
*.pfx
.claude/settings.local.json
.claude/scheduled_tasks.lock
CLAUDE.local.md
dist/
node_modules/
*.xlsx

# Built React console (docs/UI_REWRITE_PLAN.md §9 Phase 0.5) - generated by
# `npm run build` in frontend/, output lands here per vite.config.ts's
# build.outDir. Shipped in the distributed Python package, not committed to
# the repo, same as every other generated artifact in this list.
backend/src/agent_control/static/
